#include <iostream>
#include <cstring>

using namespace std;

char *resize(const char *str, int size, int new_size)
{
    for (int i = 0; i < size; i++)
        cout << *str + i;
}

int main()
{
    int n;
    cin >> n;
    char *str = new char[size];

    resize(*str, size, new_size);
    // делайте без unit-тестов
}