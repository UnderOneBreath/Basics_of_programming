            // for (int k = (bin / n) + 1; k < n; k++) {
            //     res += b[k];
            // }
            
            // for (int k = bin + 1; k / n == bin / n; k++) {
            //     res += a[k];
            // }